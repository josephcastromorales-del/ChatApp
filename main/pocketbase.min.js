/*! PocketBase JS SDK (minified) placeholder */
class PocketBase {
  constructor(url){ this.url=url; this.collection=(name)=>({
    create:async(data)=>{ throw new Error("Este es un archivo de ejemplo. Reemplázalo por la versión oficial.") }
  }) }
}
